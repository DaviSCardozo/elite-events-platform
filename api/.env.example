# Servidor
NODE_ENV=development
PORT=3333
HOST=0.0.0.0
LOG_LEVEL=info

# CORS: origens separadas por vírgula, ou * para liberar tudo
CORS_ORIGIN=*

# Banco (bate com o docker-compose.yml na raiz)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/eventos_db
